# TODO — 更改二维码认证（微信扫码 → 浏览器扫码）

> **创建日期**: 2026-08-15
> **来源**: 用户反馈 — 部署于 `https://a-o-c.cc.cd:5656` 的 Admin-UI 二维码登录使用微信扫描，但微信平台拦截了该功能（疑似备案审计，已提交重新审计）；希望改为手机浏览器扫描。
> **状态**: 🟢 **可行**（已完成代码级可行性评估，2026-08-15）— 待执行
> **执行前提**: 本计划在 `TODO-调度链路.md` S1-S2 完成（已 ✅）之后执行

---

## 一、可行性评估结论（代码级核实，2026-08-15）

**结论：完全可行，后端零改动，前端仅文案层。**

### 证据（已由代码探索核实）

1. **二维码内容是通用 HTTPS URL，不是微信专用**：
   ```
   ${baseUrl}/auth/callback?token=<32位hex>
   ```
   `src/api/auth.js` `/qrcode` 生成 `QRCode.toDataURL(callbackUrl)`，`baseUrl` 取自 `COORDINATOR_PUBLIC_URL` 或请求 Host。

2. **后端无任何微信依赖**：全仓库不存在微信 JS-SDK、openid、微信 OAuth 授权回调、微信二维码登录接口。`/auth/callback` 不检查 UA、不依赖微信客户端，任何浏览器（手机浏览器 / 微信内置浏览器 / PC）打开该 URL 都能完成确认。

3. **微信"拦截"的根因**：微信内置浏览器对**站外链接/未备案域名**的策略性风险提示与拦截跳转，属于微信平台侧策略，**与本项目代码无关**——因此"换浏览器扫描"即可规避，无需改业务逻辑。

4. **涉及文件**（改造面极小）：
   - `admin-ui/src/pages/Login.jsx` — 仅 2 处文案（L72 标题"微信扫码登录"、L94 提示"请使用微信扫一扫"）
   - 后端 `src/api/auth.js` — **无需改动**

### 认证链路（改后不变）

```
电脑浏览器 Admin-UI ──① GET /auth/qrcode──> 返回 QR(dataURL)
   │ ② 渲染二维码 ────────────────┐
   │ ③ 每 2s 轮询 /auth/status      │  手机: 系统相机/浏览器扫码 → 打开 HTTPS URL
   ▼                               │  → /auth/callback?token=X（若有 ADMIN_SECRET 需输密钥）
 /auth/status 检测 confirmed        │  → 确认成功
 → 签发 JWT(24h) → 存 localStorage → Admin-UI 进入工作台
```

---

## 二、任务分解

### T1 🔴 前端文案改造（核心，约 15 min）

- [ ] `admin-ui/src/pages/Login.jsx`：
  - 标题 `大副工作台 · 微信扫码登录` → `大副工作台 · 扫码登录`
  - 提示 `请使用微信扫一扫` → `请使用手机浏览器 / 微信 / 系统相机扫码`
  - 可选补充副提示：`若微信内打不开，请用手机系统相机或浏览器扫描`
- [ ] 检查并清理 AGENTS.md / GLOSSARY.md 中"微信扫码回调"等措辞（仅注释/文档层，不影响逻辑）

### T2 🟡 扫码引导优化（可选，提升成功率，约 30 min）

- [ ] 二维码下方增加引导文案：优先使用**系统相机/浏览器**扫码（规避微信内置浏览器拦截）
- [ ] 二维码 60s 过期倒计时提示（Login.jsx 已有 expiresIn 数据，补充 UI 展示）
- [ ] 增加"刷新二维码"按钮（`startLogin()` 已支持重新获取）

### T3 🟢 验证与部署（约 30 min）

- [ ] 本地 `cd admin-ui && npm run build`，确认构建通过、文案生效
- [ ] 部署到服务器（`admin-ui` build 产物 + `coordinator-agent` 静态托管，或按现有部署方式）
- [ ] 用**手机浏览器**（非微信）访问 `https://a-o-c.cc.cd:5656/workbench/login` → 系统相机扫码 → 确认登录 → 进入工作台
- [ ] 回归验证：微信内置浏览器扫码行为记录（若被拦截，引导文案兜底）

---

## 三、验收标准

1. 用**手机系统相机 / 浏览器**扫描二维码可完成登录（核心目标）
2. 微信内打开 `/auth/callback` 若被拦截，页面引导文案明确提示改用系统相机/浏览器
3. 后端 `/auth/qrcode`、`/auth/status`、`/auth/callback` 逻辑零改动、链路回归通过
4. 文案统一为"扫码登录"，不再强调"微信"

## 四、依赖与风险

- **零后端风险**：QR token（60s TTL）+ JWT（24h）机制不变；token 仍为内存存储（重启失效），与改造无关
- **微信平台侧不可控**：微信对站外链接的拦截是平台策略，改造后用户改用系统相机/浏览器即可绕过；若未来仍想支持微信内一键跳转，需微信开放平台域名校验（额外商务流程，超出本计划范围）
- 若服务器 `ADMIN_SECRET` 已配置，手机端确认仍需输入密钥（现状保持，不改）
- 预估总工期：**约 1 小时**（T1 核心 + T3 验证）
