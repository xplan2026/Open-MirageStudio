# 工作日志 — 2026-08-10

## 主题：探索与集成贝锐花生壳 Drop / HSK CLI

### 1. 探索花生壳 Drop 平台

- 访问 `https://ficp.fun/`（重定向至 `drop.ficp.fun/`），通过页面 HTML 源码、API 端点、搜索引擎全面探索
- 文档化探索结果：`docs/reference/花生壳-drop.md`
  - 产品定位：零门槛网页发布工具，拖拽 HTML → 生成可公网访问 URL
  - 技术栈：Vue 3 + Arco Design + Vite，后端项目 `console-onion`
  - 核心功能：拖拽上传、目录打包、长期有效（认领后）、AI 集成（WorkBuddy）
  - 适用场景：Mirage-Studio Admin-UI 演示、MV 预览页、小说章节 HTML 预览等
  - 定价：未公开，可能处于免费推广期，代码存在 `serviceExceedsLimit` 用量限制

### 2. 安装与配置 HSK CLI

- 阅读 `https://hsk.oray.com/doc/cli-setup.md` 官方文档
- 全局安装 `@aweray/hsk-cli` v0.4.3（npm + 二进制自动下载至 `~/.hsk/bin/`）
- 三条核心命令验证：
  - `host` — 文件/目录托管（生成公网链接）
  - `deploy` — 构建前端项目并托管
  - `tunnel` — 内网穿透（端口映射）

### 3. 环境变量与凭证

- `.env` 新增 `HSK_API_KEY=sk_51346f267aa9b8f38324f1744845908c`
- 卡片标记到花生壳附录文档

### 4. Admin-UI 部署到花生壳

#### 首次部署（静态托管）
- 构建 `coordinator-agent/admin-ui/` + `hsk-cli +deploy` → `https://ficp.fun/s/QD7DsU?verify_code=6086`
- 资源 ID：`1786373901495719259`
- 认领成功，分配域名 `https://ficp.fun/s/QD7DsU/`
- **问题**：只有背景 + favicon，无内容 — SPA 依赖后端 API，花生壳只托管静态文件

#### 二次部署（隧道 + 前后端打通）
- 启动 Coordinator-Agent 后端（PID 10578，port 3100）
- `hsk-cli +tunnel --ip 127.0.0.1 --port 3100 --detach` → 隧道 `https://b25c68c8622b.hz-1.aicp.space?verify_code=9729`
- 修改 `api.js`：`API_BASE` 从 `/admin` 改为隧道地址
- `hsk-cli +deploy --resource-id ...` 更新前端
- **结果**：页面仍未显示内容（隧道资源需浏览器激活）

### 5. 变更文件

| 文件 | 类型 | 说明 |
|------|------|------|
| `docs/reference/花生壳-drop.md` | 新增 | 花生壳 Drop 探索文档 |
| `.env` | 修改 | 新增 `HSK_API_KEY` |
| `coordinator-agent/admin-ui/src/api.js` | 修改 | `API_BASE` 改为隧道地址 |
| `coordinator-agent/admin-ui/vite.config.js` | 修改 | 添加注释 |

### 6. 遗留事项

- [ ] 隧道资源 `https://b25c68c8622b.hz-1.aicp.space?verify_code=9729` 需浏览器激活
- [ ] Admin-UI 页面内容需在后端 + 隧道均激活后验证
- [ ] `api.js` 中的隧道地址应在部署 `deploy.sh` 脚本中动态替换，而非硬编码
- [ ] 长远方案：4C4G 服务器部署 Coordinator-Agent 后端 + Admin-UI 静态文件共存
