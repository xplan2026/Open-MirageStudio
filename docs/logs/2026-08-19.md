---
Updated: 2026-08-20 14:25
生命周期: 永久保存
保存位置: docs/logs/2026-08-19.md
---

# 工作日志 - 2026-08-19

## 概述

数据存储改造进入 **B1 方案落地**（commit `a894de5`）：**弃用 KV，媒体改 git 中转 + CI 拷贝**。官网（mirage.cc.cd）媒体链路确定为 **Pages 原生静态资产托管**（原生 Range 206、无 25MB 单值限制），文本仍走 D1 `mirage-meta`。随后按 `docs/部署记录/website部署方案.md`（**权威方案**）完成部署，**官网已可访问媒体实体**；**两个产端（服务器 git 中转发布端、官网 Cloudflare Pages 托管端）均已按方案正确部署**。

> 注：2026-08-17 日志中的 KV 方案已被取代，以 `docs/部署记录/website部署方案.md` 为准。

## 完成事项

### 1. 方案决策：KV → 方案 B（媒体改 Pages 静态资产）✅
- 原 KV 方案（2026-08-17）存在两个硬伤：KV 单值 **25MB 上限**（逼近当前最大 mp4 13.9MB）+ 需维护**自研 Range 切片**逻辑
- **方案 B**：媒体 → Pages 静态资产（`data/` 随 CI 拷入 `dist/data/`），原生 Range 206、零函数开销、无大小限制
- 方案文档：`docs/部署记录/website部署方案.md`（**权威文档**，此后部署以此为准）

### 2. CI 改造（deploy-website.yml）✅
- 触发条件：增加 `scripts/**` + `data/**`（Agent 产物 push `data/` 即自动重建官网）
- 新增 **Copy real media into dist** 步骤：`rm -rf dist/data && cp -r data dist/data`，媒体随构建产物由 Pages 静态托管（覆盖 `.data-build/` 占位空文件）

### 3. 脚本改造 ✅
- `scripts/build-data.mjs`：改为「git `data/` 基线 + D1 文本重建」（D1 为文本权威源，媒体以 git `data/` 为权威源）
- `scripts/migrate-data.js`：移除 KV 上传模式（migrateKV 删除）
- `scripts/publish.js`：移除 KV/R2 发布逻辑
- 删除 `scripts/verify-kv.js`、`website/functions/data/[[path]].js`（KV 直读 Function 退役）
- `website/wrangler.toml`：移除 `MEDIA_KV` 绑定，注释更新为方案 B

### 4. 部署文档 ✅
- 🆕 `docs/部署记录/website部署方案.md`（165 行）：Website 部署权威方案（架构、CI/CD、发布链路、操作手册、变更记录）
- 🆕 `docs/部署记录/admin-UI部署方案.md`（188 行）：admin-ui 部署方案
- 两个历史部署记录（2026-08-11 / 2026-08-15）补充 B1 关联说明

### 5. 部署完成确认 ✅（2026-08-20 补充记录）
- 已按 `website部署方案.md` 完成部署：**官网（mirage.cc.cd）可访问媒体实体**（歌曲/MV 播放 + Range 206）
- **两个产端均已按方案正确部署**：
  - 服务器产端（4C4G `/opt/mirage-studio`）：admin-ui 运行时 Agent 产物 → git 中转（`git add data/` + push），不直连 CF，凭证不落地服务器
  - 官网产端（Cloudflare Pages `mirage-studio-website`）：CI 统一发布（build-data + 媒体拷贝 + pages deploy），静态托管 + D1 文本

## 阻塞

- 原 KV 阻塞（Cloudflare API Token 401 / KV namespace 创建失败）随方案变更**解除**：方案 B 不再需要 KV，媒体走 git + Pages 静态托管
- 发布链路仍需 `CLOUDFLARE_API_TOKEN / CLOUDFLARE_ACCOUNT_ID / D1_DATABASE_ID`（D1 文本 + Pages deploy），已配置于 GitHub Secrets

## 未完成任务（下一步）

1. 清理服务器 website 残留：nginx 兜底站点、`/data/` alias 回源、`website/dist/` 目录（确认无引用后退役）
2. P2 预留：未来高清 MV 超 GitHub 100MB 单文件上限时，评估迁移对象存储（Supabase / R2）

## 文件变更（commit `a894de5`）

| 文件 | 变更 |
|------|------|
| `.github/workflows/deploy-website.yml` | 触发加 data/**、scripts/**；新增媒体拷贝步骤 |
| `docs/部署记录/website部署方案.md` | 🆕 权威部署方案（165 行） |
| `docs/部署记录/admin-UI部署方案.md` | 🆕 admin-ui 部署方案（188 行） |
| `docs/部署记录/2026-08-11-admin-ui-部署.md` / `2026-08-15-github-actions-自动部署.md` | 补充 B1 关联 |
| `scripts/build-data.mjs` | git data/ 基线 + D1 文本重建 |
| `scripts/migrate-data.js` | 移除 KV 模式 |
| `scripts/publish.js` | 移除 KV/R2 发布逻辑 |
| `scripts/verify-kv.js` | 🗑️ 删除 |
| `website/functions/data/[[path]].js` | 🗑️ 删除（KV Function 退役） |
| `website/wrangler.toml` | 移除 MEDIA_KV 绑定 |

## 收工

- **状态**: B1 方案落地 + 部署完成 — 两个产端均按方案正确部署，官网可访问媒体实体 ✅
- **下一步**: 清理服务器 website 残留；监控官网媒体访问（Range 206）
