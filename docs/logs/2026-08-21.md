---
Updated: 2026-08-21 05:10
生命周期: 永久保存
保存位置: docs/logs/2026-08-21.md
---

# 工作日志 - 2026-08-21

## 概述

Erhu Agent **数字人改造**（详见 `erhu-agent/docs/数字人改造计划-2026-08-21.md`）完成全部**不依赖新凭据**的实施项，剩余项进入「等待介入」。核心成果：二虎形象重塑为**西藏男性民谣歌手**、新增 mvsep 音轨分离（`stem.js`）与 wan2.2-s2v 对口型（`s2v.js`）两个脚本、`render.js` 支持对口型段 + 空镜段混合渲染。

## 完成事项

### 1. 音轨分离调研收尾 ✅
- 删除 3 个本地音轨分离调研文档（`docs/reference/` 下 Spleeter / UVR / demucs），仅保留 mvsep 在线服务（`docs/reference/mvsep操作指南.md`）

### 2. 二虎形象重塑为西藏男性 ✅
- `erhu-agent/assets/erhu_profile.json`：`prompt_template` 改为西藏男性（国字脸、高原古铜肤色、藏式服饰与配饰）
- 重新生成 4 张西藏形象图（`erhu-agent/assets/`）：`erhu_front_tibet_test` / `erhu_front_half_tibet` / `erhu_left_15_tibet` / `erhu_right_15_tibet`
- ⚠️ `eyes_closed`（正面闭眼深情）第 5 张**待生成**，待审图确认后补齐

### 3. 音轨分离模块 `scripts/stem.js` ✅
- 封装 mvsep API（`MVSEP_API_TOKEN`），分离人声轨 vocals.wav / 伴奏 other.wav
- **测试通过**：对《此身长在画图间》分离成功，产物 `data/Erhu-data/此身长在画图间/.stem/`（vocals + other，各 37MB）

### 4. 对口型模块 `scripts/s2v.js` ✅
- 封装阿里云百炼 wan2.2-s2v API（`DASHSCOPE_API_KEY` + `DASHSCOPE_WORKSPACE_ID`）：上传凭证 → 创建任务 → 轮询 → 下载
- 实现音频切段（默认 ≤20s 上限，`--segment` 可调）+ 多段视频拼接（形象图仅上传一次复用）

### 5. `render.js` 混合渲染改造 ✅
- 新增混合渲染模式：存在 `{作品名}/s2v/` 对口型片段时，按 `--s2v-dir` / `--s2v-start` 顺序铺对口型段（副歌），其余时间用图片 Ken Burns 空镜补齐
- 无 s2v 片段时保持原纯空镜逻辑；`--dry-run` 可打印混合时间线

### 6. 文档同步 ✅
- `erhu-agent/docs/数字人MV技术方案.md`：补充 wan2.2-s2v 方案、西藏形象、mvsep 音轨分离
- `erhu-agent/AGENTS.md` / `GLOSSARY.md` / `TODO.md`：技术栈、工作流、目录结构、依赖环境变量、Phase 8 进度

两个脚本均通过 `node --check` 语法校验，无 lint 错误。

## 等待介入（阻塞明细）

| # | 事项 | 需要用户做什么 |
|---|------|---------------|
| 1 | **P4.1 阿里云百炼凭据** | 提供 `DASHSCOPE_API_KEY` + `DASHSCOPE_WORKSPACE_ID`（华北2北京地域），写入 `.env` |
| 2 | **P2.3 西藏形象图审图** | 确认 4 张西藏形象图是否满意；确认后补齐 `eyes_closed` 第 5 张 |
| 3 | **P4.4 对口型单段测试** | 依赖 #1 凭据，测试 15s 人声 → 对口型视频 |
| 4 | **P5.3 端到端完整 MV** | 依赖 #1 + #2，跑通「音轨分离 → 对口型 → 混合渲染」整链 |
| 5 | **预算确认** | wan2.2-s2v 按秒计费（480P 0.5 元/秒），整首 3 分钟约 90 元；建议先做 1 分钟试听段验证 |

## 文件变更

| 文件 | 变更 |
|------|------|
| `docs/reference/{Spleeter,UVR,demucs}-音轨分离工具调研.md` | 🗑️ 删除（保留 mvsep） |
| `erhu-agent/assets/erhu_profile.json` | prompt 改西藏男性 |
| `erhu-agent/assets/erhu_*_tibet*.jpg` | 🆕 4 张西藏形象图 |
| `erhu-agent/docs/数字人改造计划-2026-08-21.md` | 🆕 改造计划 |
| `erhu-agent/docs/数字人MV技术方案.md` | 补 wan2.2-s2v / 西藏形象 / mvsep |
| `erhu-agent/scripts/stem.js` | 🆕 音轨分离 |
| `erhu-agent/scripts/s2v.js` | 🆕 对口型（切段+拼接） |
| `erhu-agent/scripts/render.js` | 混合渲染 |
| `erhu-agent/AGENTS.md` / `GLOSSARY.md` / `TODO.md` | 同步更新 |

## 收工

- **状态**: 数字人改造「完成 + 等待介入」— 不依赖凭据的实施项全部落地 ✅，剩余 5 项阻塞待用户介入
- **下一步**: 用户提供阿里云百炼凭据（`DASHSCOPE_API_KEY` + `DASHSCOPE_WORKSPACE_ID`）+ 审图确认后，进入对口型端到端测试
