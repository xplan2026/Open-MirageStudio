# 工作日志 - 2026-08-15

## 概述

生产调度链路（S1-S4）审计修复后部署上线，随后打通**服务器自动部署 CI/CD**（S4a）——新增 GitHub Actions `deploy-server.yml`，agent 代码推送 main 即自动同步服务器 + 重启 coordinator，解决 S5 前置的服务器旧代码阻塞。

## 完成事项

### 1. S1-S4 调度链路（此前已完成，详见 `TODO-调度链路.md`）
- S1 真实分发 + 适配器、S2 适配器 AGENT_ROOT 收口、S3 xujie 人控适配器、S4 服务器部署
- 4C4G 服务器（`182.254.180.26`）PM2 运行 coordinator

### 2. 服务器代码同步排查
- `git reset --hard` 失败根因：S4 `sudo tar` 解压致部分文件属 root → 修复所有权
- 服务器到 GitHub git 认证问题：HTTPS 通、私有仓库 token 认证 → 重建 remote 后 fetch 成功
- 现有 CI/CD 评估：`deploy-website.yml`（Cloudflare Pages）仅覆盖静态网站，**不适用** Express 服务部署

### 3. S4a 服务器自动部署 CI/CD ✅（今日核心）
- **GitHub Secrets（5 个）**: `SERVER_SSH_KEY`（复用 `xplan_server_key`）+ `SERVER_HOST`/`SERVER_USER`/`SERVER_PORT`/`SERVER_DEPLOY_PATH`（libsodium sealedbox 加密写入）
- **`.github/workflows/deploy-server.yml`** 🆕：rsync 推送 5 个 agent 目录（排除 node_modules/.env/tasks）+ `data/` 仅追加 + `npm ci` + `pm2 restart coordinator` + 健康检查
- **首次部署验证**: Actions run #1 **success**；服务器 `/health` ok、`acknowledgeTask`/`verify-scheduler.mjs` 新代码就位、PM2 online
- commit `1633687` 双推 cnb.cool + GitHub

### 4. 文档更新
- `TODO-调度链路.md`：新增 S4a 小节、S5 阻塞项标记已解决、进度行更新
- `TODO.md`：新增 71d 条目、最后更新行补充
- 新建部署记录 `docs/部署记录/2026-08-15-github-actions-自动部署.md`

## 文件变更

| 文件 | 变更 |
|------|------|
| `.github/workflows/deploy-server.yml` | 🆕 自动部署 workflow |
| `docs/部署记录/2026-08-15-github-actions-自动部署.md` | 🆕 部署记录 |
| `docs/logs/2026-08-15.md` | 🆕 今日日志 |
| `TODO-调度链路.md` | S4a 小节 + S5 阻塞更新 |
| `TODO.md` | 71d + 最后更新行 |

## 风险备忘

- `SERVER_SSH_KEY` 为主密钥（非独立 deploy key），后续建议轮换
- rsync `--delete` 仅作用于 agent 目录，`data/`/`tasks/`/`.env` 受保护

## 收工

- **状态**: S4a ✅ 完成，S5 待继续（服务器已就绪）
- **下一步**: S5 端到端验证（verify-scheduler.mjs 冒烟 15/15 + REST 真实任务执行）
