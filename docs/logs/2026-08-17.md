# 工作日志 - 2026-08-17

## 概述

官网（mirage.cc.cd）媒体详情页访问问题的**数据存储改造**。核心链路：媒体数据位于服务器 `/data/` 目录，与 Cloudflare Pages 分离，代理链路被 Cloudflare 限制（`_redirects` 不支持外部域名、Function 禁裸 IP、回源域名 429）。确定改造方案为 **KV（媒体）+ D1（文本）**，已完成后端全部代码与 CI 改造；**今日卡点在 Cloudflare API Token 失效**，KV namespace 创建与媒体上传待凭证恢复后继续。

## 完成事项

### 1. 登录页二维码改浏览器扫码 ✅
- `coordinator-agent/admin-ui/src/pages/Login.jsx` 文案调整，已部署上线（前序完成，随今日提交）

### 2. 方案确定与文档（`TODO-数据存储改造.md` 🆕）
- 原路线 A：R2（媒体）+ D1（文本）
- **阻塞**：账户无任何可用支付方式，无法开通 R2（API 10042）→ **方案变更为免费 KV + D1**
- TODO 文档已重写记录方案变更

### 3. D1 文本元数据层 ✅ 全部完成
- 数据库 `mirage-meta`（ID `ac463a4d-61ff-42bf-a56e-aeb89d7cd82a`）
- 6 表全部建好并迁移验证完整：`songs:5` / `mv_projects:3` / `novels:1` / `chapters:10` / `families:5` / `blog_posts:4`
- `scripts/build-data.mjs`：D1 → 文件系统重建验证通过（168 个 HTML，含 5 音乐 + 3 MV 详情页）
- `website/src/lib/data.ts` 支持 `DATA_BUILD_DIR`
- `.gitignore` 加入 `website/.data-build/`

### 4. CI 改造 ✅
- `.github/workflows/deploy-website.yml`：移除 `data/**` 触发；新增 build-data 步骤（D1 → 数据目录）；新增 `workflow_dispatch` 手动触发（供 publish.js 调用）
- CI 依赖 GitHub Secrets：`CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_ACCOUNT_ID` / `D1_DATABASE_ID`

### 5. KV 替代方案代码 ✅ 编写完成
- `website/wrangler.toml`：KV 绑定 `MEDIA_KV`（namespace id 待回填）
- `website/functions/data/[[path]].js`：KV 直读优先 + 回源降级 + 自实现 Range 206（播放器 seek 支持）
- `scripts/publish.js`：R2 API → KV API，新增 `--no-deploy` 参数
- `scripts/migrate-data.js`：新增 `--kv` 模式（媒体上传，自动创建 namespace，>25MB 报错跳过）
- 四 Agent 接入（写盘后自动发布云端，失败不阻塞）：
  - `lemong-agent/scripts/generate-music.js`（歌曲生成后发布）
  - `erhu-agent/scripts/produce.js`（MV 渲染后发布）
  - `zhupu-manager-agent/zhupu-manager-agent`（init/add/motto/emblem 发布族谱）
  - `xujie-writer-agent/.../WORKFLOW.md`（第九步云端发布指引）

## 阻塞 ⛔

**Cloudflare API Token 失效（HTTP 401 Authentication error）**

- `.env` 中 `CLOUDFLARE_API_TOKEN`（`cfut_1` 前缀）调用 KV API 返回 401
- 已排查所有凭证来源均无有效凭证：
  - `.env`（失效）/ `.codebuddy/.env.codebuddy`（无 Cloudflare 配置）
  - shell 环境变量（无）/ IDE 会话凭证（非 Cloudflare 凭证，404）
  - 服务器 `/opt/mirage-studio/.env` 及 `coordinator-agent/.env`（仅有 DeepSeek/Zhipu/ACE/BAIDU 等，无 Cloudflare）
- 影响：KV namespace `mirage-media` 创建失败（`wrangler.toml` id 仍为占位符 `<创建后填入>`）、媒体上传无法执行
- 今日 11:11 仍有用 wrangler `d1 execute` 成功记录，疑似 token 曾有效后被轮换/过期

## 未完成任务（下一步）

1. **获取有效 Cloudflare API Token**（需 Workers KV + Pages + D1 权限，Dashboard → My Profile → API Tokens 创建），更新 `.env`
2. 运行 `node scripts/migrate-data.js --kv`：自动创建 KV namespace `mirage-media` → **回填 `website/wrangler.toml` 的 id** → 上传全部媒体（mp3/mp4/图片）
3. 本地启动/构建验证 Function KV 直读（`npm run dev` 冒烟）
4. 推送代码 + 触发/部署 CF Pages，端到端验证详情页播放（Range 206）
5. Phase 6：`data/` 目录弃用 + 文档更新（CODEBUDDY.md 数据目录说明等）
6. 清理本地 `website/.data-build/`

## 文件变更（本批次待提交）

| 文件 | 变更 |
|------|------|
| `TODO-数据存储改造.md` | 🆕 KV 方案文档 |
| `website/wrangler.toml` | 🆕 KV + D1 绑定 |
| `website/functions/data/[[path]].js` | 🆕 KV 直读 Function |
| `scripts/`（build-data.mjs / publish.js / migrate-data.js / verify-kv.js） | 🆕 构建与发布脚本 |
| `.github/workflows/deploy-website.yml` | build-data 步骤 + workflow_dispatch |
| `.gitignore` | `website/.data-build/` |
| `website/src/lib/data.ts` | `DATA_BUILD_DIR` 支持 |
| `website/public/_redirects` | 代理规则调整 |
| 四 Agent 文件（lemong/erhu/zhupu/xujie） | 发布器接入 |
| `coordinator-agent/admin-ui/src/pages/Login.jsx` | 二维码扫码文案（已上线） |

## 收工

- **状态**: 后端代码与 CI 全部就绪，阻塞在 Cloudflare 凭证
- **下一步**: 用户提供有效 API Token → 执行 KV 迁移 → 部署验证
