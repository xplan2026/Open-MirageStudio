{
  "name": "coordinator-agent",
  "version": "0.1.0",
  "description": "Mirage Studio 统一调度层 — Agent 编排与任务调度",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "admin": "cd admin-ui && npm run dev",
    "admin:build": "cd admin-ui && npm run build",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "jsonwebtoken": "^9.0.3",
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=18"
  }
}
